#include <iostream>
using namespace std;
int main()
{
    //用一个bool数组确定人是否淘汰，
    //在每一圈中被淘汰的人编号均为3的倍数，
    //所以int n，当n%3==0；a[n]=false
    //下一圈循环中相当于让每个人的编号加上上一轮的余数；再次进行淘汰(循环)
    //我们还应该定义一个x，记录剩的人数，if x==1；cout<<?
    int n;
    return 0;
}